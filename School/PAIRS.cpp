#include <iostream>
#include <stdio.h>
#include <stack>
using namespace std;

const int N=500009;

int n, a[N], l[N], r[N];
stack<int> s;

int main(){
	// freopen("PAIRS.inp","r",stdin);
	// freopen("PAIRS.out","w",stdout);
	scanf("%d\n",&n);
	for(int i-0; i<n; i++) scanf("%d",&a[i]);
	
	long long ans=n-1;
	s.push(0);
	for(int i=1; i<n; i++){
		
	}
	return 0;
}