#include <cstdio>
using namespace std;

const int N=1000009;

int n, p, a[N]={};

int main(){
    // froepen("Dolls.inp","r",stdin);
    // freopen("Dolls.out","w",stdout);
    scanf("%d",&n);
    for(int i=0; i<n; i++) scanf("%d",&a[i]);
    scanf("%d",&p);

    int l = -1, r = 0, ans = 0,;
    while(r < n){

    }
    return 0;
}
