Uses Math;
Var st:ansistring;
//---------------------------------------------------------
Function ok:boolean;
	Var i:longint;
	Begin
		for i:=1 to length(st)-1 do
			Begin
			
			End;
	End;
//=========================================================
BEGIN
	readln(st);
	if ok then write('YES')
	else write('NO');
END.